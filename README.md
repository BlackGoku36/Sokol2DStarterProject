Uses [zig](https://ziglang.org/download/) as build systems (you can use your own if you want to).
Currently only build for windows (opengl) and macos (metal), but adding more is easy (PR appreciated :)).